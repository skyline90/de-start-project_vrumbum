/*Добавьте в этот файл пояснения к выбранной модели данных. 
Также вы можете сохранить здесь другие комментарии по проекту, которые вы считаете необходимыми, 
и задать вопросы ревьюеру*/
Выполнение проекта состояло из след. этапов:
1. создание схемы raw_data и таблицы sales в ней? выгрузка туда сырых данных через psql терминал(initial.sql);
2. создал схему car_shop, проанализировав сырые данные пришел в выводу что необходимо в обязательном порядке создать 3 отдельные таблицы, для машин(cars), покупателей(customers) и сводной таблицы продаж(sales) это было выполнено в ddl.sql;
3. загрузил данные из сырой таблицы raw_data в соответствующие таблицы файл INSERT.SQL;
4. выполнение задач по анализу данных выполнено в task_1.sql - task_6.sql

После вашего ревью были учтены и исправлены след. комментарии:
1. при создании таблиц было добавлено проверка отсутствия таких таблиц IF NOT EXISTS,
2. при создании таблиц изменил тип данных для колонок с TEXT на VARCHAR,
3. при использовании JOIN явно указал метод присоединения INNER JOIN,
Исправлена ошибка в 1ом задании:
- для вычисления процента машин без указанного параметра было применено использование подзапросов

Спасибо, за ревью и коментарии.
